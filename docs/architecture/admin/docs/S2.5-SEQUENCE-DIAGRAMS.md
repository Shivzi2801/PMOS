# PMOS — Wave 2 — Slice S2.5

# Administration & Governance Module — Sequence Diagrams

> Package: `backend/admin/`
> Version: `2.5.0`

These diagrams trace the principal flows through the slice. They use simple ASCII
sequence notation; participants are the service objects described in the architecture
document. The recurring pattern — **validate → mutate → metric → audit** — is visible in
nearly every write flow.

---

## 1. Configuration Update (with optimistic concurrency)

```
Caller        ConfigurationService     Validator      Registry        Metrics   Audit
  │                  │                     │             │              │         │
  │ update(scope,    │                     │             │              │         │
  │   key, settings, │                     │             │              │         │
  │   expected_ver)  │                     │             │              │         │
  ├─────────────────▶│                     │             │              │         │
  │                  │ validate(scope,set) │             │              │         │
  │                  ├────────────────────▶│             │              │         │
  │                  │  normalised | raise ConfigurationValidationError │         │
  │                  │◀────────────────────┤             │              │         │
  │                  │ update(key, norm, expected_ver)   │              │         │
  │                  ├──────────────────────────────────▶│              │         │
  │                  │   version match? ── no ─▶ ConfigurationVersionConflictError │
  │                  │   yes: append v+1, checksum       │              │         │
  │                  │◀──────────────────────────────────┤              │         │
  │                  │ increment(CONFIG_CHANGES)         │              │         │
  │                  ├──────────────────────────────────────────────────▶        │
  │                  │ record(AuditEvent CONFIGURATION)                 │         │
  │                  ├───────────────────────────────────────────────────────────▶
  │  new version     │                                                            │
  │◀─────────────────┤                                                            │
```

---

## 2. Configuration Rollback

```
Caller        ConfigurationService        Registry                 Audit
  │                  │                        │                       │
  │ rollback(key, v) │                        │                       │
  ├─────────────────▶│ get_version(key, v)    │                       │
  │                  ├───────────────────────▶│                       │
  │                  │  settings@v | NotFound  │                       │
  │                  │◀───────────────────────┤                       │
  │                  │ update(key, settings@v) → appended as new head  │
  │                  ├───────────────────────▶│                       │
  │                  │◀───────────────────────┤                       │
  │                  │ record(CONFIGURATION "rollback to v")           │
  │                  ├────────────────────────────────────────────────▶
  │  new head (=v')  │                                                 │
  │◀─────────────────┤                                                 │
```
History is preserved: the rollback is itself a forward, audited version.

---

## 3. Effective Configuration Resolution (read)

```
Caller        ConfigurationService            Registries (system/tenant/workspace)
  │                  │                                  │
  │ effective_config(tenant, workspace)                │
  ├─────────────────▶│ get system head                 │
  │                  ├────────────────────────────────▶│
  │                  │ get tenant head (if any)         │
  │                  ├────────────────────────────────▶│
  │                  │ get workspace head (if any)      │
  │                  ├────────────────────────────────▶│
  │                  │ deep_merge(system ⊕ tenant ⊕ ws) │
  │  merged settings │                                  │
  │◀─────────────────┤                                  │
```

---

## 4. Feature Flag Evaluation (hot path)

```
Caller       FeatureFlagService        FeatureFlag            Metrics
  │                │                       │                    │
  │ is_enabled(    │                       │                    │
  │  key, subject) │                       │                    │
  ├───────────────▶│ flag exists? ── no ─▶ return False         │
  │                │ yes:                   │                    │
  │                │ evaluate(subject)      │                    │
  │                ├──────────────────────▶│                    │
  │                │   workspace override?  │                    │
  │                │   else tenant override?│                    │
  │                │   else rollout bucket (sha256(key:subj)%100)│
  │                │   else global enabled  │                    │
  │                │◀──────────────────────┤                    │
  │                │ increment(FEATURE_FLAG_EVALUATIONS)         │
  │                ├────────────────────────────────────────────▶
  │  bool          │                                             │
  │◀───────────────┤                                             │
```

---

## 5. Access Policy Evaluation

```
Caller         PolicyEngine                              Metrics
  │                │                                        │
  │ evaluate_access(context)                                │
  ├───────────────▶│ select applicable policies (scope)     │
  │                │ sort by priority asc                    │
  │                │ for each policy, for each rule:         │
  │                │   if rule matches context → effect wins │
  │                │ no match → policy.default_effect (DENY) │
  │                │ increment(POLICY_EVALUATIONS)           │
  │                ├────────────────────────────────────────▶
  │ PolicyDecision │                                         │
  │ (effect,policy,│                                         │
  │  rule, reason) │                                         │
  │◀───────────────┤                                         │
```

---

## 6. Quota Check & Consume

```
Caller          QuotaService                 Metrics      Audit
  │                  │                          │            │
  │ check_and_consume(scope, amount, ctx)       │            │
  ├─────────────────▶│  ── acquire lock ──       │            │
  │                  │ resolve most-specific quota            │
  │                  │ no quota? ─▶ ALLOW (unlimited)          │
  │                  │ window_expired? ─▶ reset window         │
  │                  │ increment(QUOTA_CHECKS)  │            │
  │                  ├─────────────────────────▶│            │
  │                  │ current+amount ≤ limit ?               │
  │                  │   yes ─▶ record usage ─▶ ALLOW          │
  │                  │   no, enforced=False ─▶ record, ALLOW   │
  │                  │   no, enforced=True:                    │
  │                  │      increment(QUOTA_VIOLATIONS)        │
  │                  ├─────────────────────────▶│            │
  │                  │      record(SECURITY audit)            │
  │                  ├──────────────────────────────────────▶│
  │                  │   ── release lock ──                    │
  │  ok | QuotaExceededError                                  │
  │◀─────────────────┤                                        │
```

---

## 7. Tenant Creation

```
Caller     TenantAdminService     ConfigurationService    Metrics   Audit
  │              │                       │                   │         │
  │ create_tenant(name, meta)            │                   │         │
  ├─────────────▶│ new Tenant(state=PENDING)                 │         │
  │              │ create default tenant configuration       │         │
  │              ├──────────────────────▶│                   │         │
  │              │◀──────────────────────┤                   │         │
  │              │ increment(TENANT_ACTIVITY)                │         │
  │              ├──────────────────────────────────────────▶│         │
  │              │ record(TENANT "created")                  │         │
  │              ├────────────────────────────────────────────────────▶
  │  Tenant      │                                           │         │
  │◀─────────────┤                                           │         │
```

---

## 8. Tenant Lifecycle Transition (suspend example)

```
Caller     TenantAdminService                 Metrics   Audit
  │              │                               │         │
  │ suspend(tenant_id)                           │         │
  ├─────────────▶│ load tenant | TenantNotFound  │         │
  │              │ legal ACTIVE→SUSPENDED ?       │         │
  │              │   no ─▶ TenantStateError       │         │
  │              │   same state ─▶ idempotent no-op         │
  │              │   yes ─▶ set state SUSPENDED   │         │
  │              │ increment(TENANT_ACTIVITY)     │         │
  │              ├──────────────────────────────▶│         │
  │              │ record(TENANT "suspended")     │         │
  │              ├──────────────────────────────────────────▶
  │  Tenant      │                                          │
  │◀─────────────┤                                          │
```

---

## 9. Workspace Creation (with governance pre-flight)

```
Caller    WorkspaceAdminService   TenantAdminService  GovernanceService  ConfigService  Audit
  │             │                       │                  │                 │           │
  │ create_workspace(tenant, name, settings)              │                 │           │
  ├────────────▶│ get tenant            │                  │                 │           │
  │             ├──────────────────────▶│                  │                 │           │
  │             │  tenant usable? ── no ─▶ TenantStateError/NotFound          │           │
  │             │◀──────────────────────┤                  │                 │           │
  │             │ enforce(settings)      │                  │                 │           │
  │             ├──────────────────────────────────────────▶│                 │           │
  │             │  CRITICAL violation? ─▶ GovernanceViolationError (abort)     │           │
  │             │◀──────────────────────────────────────────┤                 │           │
  │             │ create Workspace(state=ACTIVE)            │                 │           │
  │             │ provision workspace configuration         │                 │           │
  │             ├──────────────────────────────────────────────────────────▶ │           │
  │             │ increment(WORKSPACE_ACTIVITY) ; record(WORKSPACE "created")  │           │
  │             ├──────────────────────────────────────────────────────────────────────▶│
  │ Workspace   │                                                                          │
  │◀────────────┤                                                                          │
```

---

## 10. Governance Validation vs. Enforcement

```
Caller        GovernanceService                         Metrics
  │                │                                       │
  │ validate(subject)                                      │
  ├───────────────▶│ for each rule: eval condition tree    │
  │                │   true → subject VIOLATES rule         │
  │                │ collect violations (never raises)      │
  │                │ if any: increment(GOVERNANCE_VIOLATIONS)│
  │                ├───────────────────────────────────────▶
  │ [violations]   │                                        │
  │◀───────────────┤                                        │
  │                │                                        │
  │ enforce(subject)                                        │
  ├───────────────▶│ validate(subject)                      │
  │                │ any CRITICAL violation ?               │
  │                │   yes ─▶ GovernanceViolationError      │
  │                │   no  ─▶ return (warnings allowed)     │
  │ ok | raise     │                                        │
  │◀───────────────┤                                        │
```

---

## 11. Audit Record & Chain Verification

```
Caller         AuditService            AuditStore
  │                 │                       │
  │ record(action)  │                       │
  ├────────────────▶│ seq = last_seq + 1    │
  │                 │ previous_hash = head   │
  │                 │ event_hash = H(canonical(event ⊕ previous_hash))
  │                 │ append(event)          │
  │                 ├──────────────────────▶│
  │  AuditEvent     │                       │
  │◀────────────────┤                       │
  │                 │                       │
  │ verify_chain()  │                       │
  ├────────────────▶│ walk events in order  │
  │                 │ recompute each hash    │
  │                 │ compare to stored      │
  │  ok | first broken sequence              │
  │◀────────────────┤                       │
```

---

## 12. Health Probe Aggregation

```
Orchestrator      HealthService              Component/Dependency checks   Metrics
  │                   │                              │                       │
  │ check_health()    │                              │                       │
  ├──────────────────▶│ for each registered check:    │                       │
  │                   │   run check (throw → UNHEALTHY)│                       │
  │                   ├──────────────────────────────▶│                       │
  │                   │◀──────────────────────────────┤                       │
  │                   │ aggregate worst-wins           │                       │
  │                   │ gauge(HEALTH_STATUS)           │                       │
  │                   ├───────────────────────────────────────────────────────▶
  │ PlatformHealth    │                                │                       │
  │◀──────────────────┤                                │                       │
  │                   │                                │                       │
  │ readiness()       │ all CRITICAL deps healthy ? → bool                     │
  │ liveness()        │ process alive ? → true                                 │
```
